import { Header } from '@/features'
import { Button } from '@/shared'

export function App() {
  return (
    <div>
      <Button size={'large'} variant={'outline'}>
        Button
      </Button>
      <Header />
    </div>
  )
}
